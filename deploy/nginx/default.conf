# HTTP — ACME challenge + reverse proxy
# After certbot, replace with default-ssl.conf or enable SSL server block below.

upstream lumc_django {
    server web:8000;
}

server {
    listen 80;
    server_name lumc-dodoma.duckdns.org;

    client_max_body_size 200M;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location /static/ {
        alias /var/www/static/;
        expires 7d;
        add_header Cache-Control "public";
    }

    location /media/ {
        alias /var/www/media/;
        expires 1d;
    }

    location / {
        proxy_pass http://lumc_django;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }
}

# Uncomment after obtaining certificates (certbot):
#
# server {
#     listen 443 ssl http2;
#     server_name lumc-dodoma.duckdns.org;
#
#     ssl_certificate     /etc/nginx/ssl/fullchain.pem;
#     ssl_certificate_key /etc/nginx/ssl/privkey.pem;
#
#     client_max_body_size 200M;
#
#     location /static/ {
#         alias /var/www/static/;
#         expires 7d;
#     }
#     location /media/ {
#         alias /var/www/media/;
#     }
#     location / {
#         proxy_pass http://lumc_django;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto https;
#         proxy_read_timeout 120s;
#     }
# }
#
# server {
#     listen 80;
#     server_name lumc-dodoma.duckdns.org;
#     return 301 https://$host$request_uri;
# }
